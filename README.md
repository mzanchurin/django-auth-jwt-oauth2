# django-auth-jwt-oauth2
Django + DRF authentication &amp; authorization service with JWT access tokens, refresh token rotation, optional 2FA, OAuth2 flows, and request rate limiting via DRF throttling.
